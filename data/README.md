# Public Content

Every JSON value in this directory is public and may be embedded in generated HTML or JavaScript.

Use only information intended for public display. Never add passwords, API keys, tokens, cookies, internal endpoints, server addresses, private contact details, or other confidential data.

Each section must keep matching structures in `en.json` and `id.json`.
